# Copy to .env.local and fill in real values. Never commit .env.local.

# Contact form: posts submissions to this Discord channel webhook as an
# interim notification channel until the Supabase-backed contact_messages
# table + admin inbox are built (see docs/05-api-design.md).
DISCORD_CONTACT_WEBHOOK_URL=

# Supabase (not wired up yet)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

NEXT_PUBLIC_SITE_URL=
