# Copy to .env.local and fill in real values. Never commit .env.local.

# Contact form's Discord webhook is currently hardcoded in
# src/lib/actions/contact.ts (trust's own call, see chat) rather than read
# from an env var here — move it back to DISCORD_CONTACT_WEBHOOK_URL if the
# repo ever becomes public or access needs tightening.

# Supabase (not wired up yet)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

NEXT_PUBLIC_SITE_URL=
